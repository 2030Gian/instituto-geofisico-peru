org: gian2030rosexp
service: api-web-scraping

provider:
  name: aws
  runtime: python3.12
  memorySize: 1024
  timeout: 30
  iam:
    role: arn:aws:iam::153620185912:role/LabRole

functions:
  scrape_table:
    handler: SCRAPING_TABLE.lambda_handler
    package:
      include:
        - SCRAPING_TABLE.py
    events:
      - http:
          path: /scrape/table
          method: get
          cors: true

resources:
  Resources:
    TablaWebScrappingIGP:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: TablaWebScrappingIGP
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
